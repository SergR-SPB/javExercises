package LevelUp4;
//Создаём класс Car для сущности Машина.
public class Car {
// Здесь распологаем свойства [и методы] которыми будут распологать обьекты
//данного класса, иными словами - переменные ->> поля класса:
    String brand;       //марка машины
    String color;       //Цвет
    double weight;      //Вес
    int wheelSize;      //Размер колес
    double maxSpeed;    //Макс скорость
    double currentSpeed;  //Текущая скорость

    // Эти переменные мы можем брать, устанавливать, исползовать внутри методов и т.д.
    // Кроме прочего класс - это тип, которые мы сможем исползовать при обявлении переменных
    // По сути мы создали тип Car

            //Начало п1. Перейдем в класс Main и положим в эти поля какиелибо значения.

            // ! [смотри  class Main]
            // ! [Создали обьекты класса Car со своимя значениями полей/переменных]

            //Конец п.1 возвращаемся в класс Car и создадим методы для типа/класса Car.

    //Создадим метод текущей скорости.
    //Для обьявления метода:
    //Мы должны вернуть тип результата нашего метода
        // Метод может 1) возвращать либо 2) не возвращать какоелибо значение.
        // 1)
    double GetCurrentspeed (/*для входных значений*/) // Получить текущую скорость
    {  /*Тело метода*/
        return currentSpeed;   //возвратить текущую скорость
    }
        // 2) Void
    void accelerate(){
        /*Метод показывает ускорение до достижения мах скорости*/
        while (currentSpeed<=maxSpeed) {
            currentSpeed += 10;
            System.out.println("currentSpeed: "+currentSpeed);
        }
    }

            //Начало п2. Перейдем в класс Main и вызовем  метод accelerate у обьекта KIA. и ford

            // ! [смотри  class Main]

            //Конец п.2 переходим опять в класс Car и создадим ещё метод
       /*метод вызывает изменение цвета машины при "currentSpeed > speed" */
            // Возвращает цвет, входной параметр - скорость изменения цвета.

    String changeColor (int speed){
        if(currentSpeed > speed) {
            color = "Grin";

        }
        return  color;
    }

            //Начало п3. Перейдем в класс Main и создадим новый чистый обьект mazda

            // ! [смотри  class Main]

            //Конец п.3 переходим опять в класс Car и _____



}
